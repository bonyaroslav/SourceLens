# Source Lens Golden Fixture

Source Lens backend vertical slice uses Qdrant local mode for vector storage.

The metadata repository is SQLite behind a repository abstraction.

The current backend milestone is backend-only; Angular UI is intentionally deferred.
